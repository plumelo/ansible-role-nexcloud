---

- hosts: all
  become: yes
  roles:
    - role: plumelo.nextcloud
      nextcloud_hosts:
        - nextcloud.local
      # nextcloud_install_database: no
      nextcloud_apps:
        - name: "direct_menu"
          src: "https://github.com/juliushaertl/direct_menu.git"

      nextcloud_config:
        - name: "enable_avatars"
          value: true
          # type: boolean

      nextcloud_users:
        - name: "newname"
          pass: "777pass555"
